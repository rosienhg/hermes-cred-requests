# Byte-compiled / caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Environments
.venv/
venv/
env/

# Packaging
build/
*.egg-info/

# NOTE: dashboard/dist/ is deliberately NOT ignored — the dashboard plugin bundle ships as-is
# (no build step), so it must be committed. Anything under it is part of the release.

# Local state this plugin writes at runtime — never commit these
credential-requests.json
credential-requests.json.corrupt

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp